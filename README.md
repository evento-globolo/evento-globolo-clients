# evento-globolo-clients

Generated and hand-written Evento Globolo clients for supported application runtimes.

Initialized through `DEN-1889` as a testable `clients` foundation. Product behavior continues through focused pull requests.

```bash
python3 scripts/verify_repo.py
```
